[project]
name = "ca_firm"
authors = [
    { name = "CA Firm", email = "jwabdms@gmail.com" }
]
description = "Complete Statutory Audit Management for Chartered Accountancy Firms, built on Frappe."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]

[tool.ruff]
line-length = 110
target-version = "py310"
